@model IEnumerable<Application_MVC.Models.Produit>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Designation)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Money)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Qte_Stock)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Designation)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Money)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Qte_Stock)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Ref_Prod }) |
            @Html.ActionLink("Details", "Details", new { id=item.Ref_Prod }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Ref_Prod })
        </td>
    </tr>
}

</table>
